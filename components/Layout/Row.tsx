import styled from "styled-components";

const Row = styled.section`
  flex: 1 1 100%;
  display: flex;
  flex-wrap: wrap;

  /* TODO: make this a dynamic variable */
  gap: 16px;
`;

export default Row;
